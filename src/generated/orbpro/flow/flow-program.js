// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { FlowEdge } from "./flow-edge.js";
import { FlowNode } from "./flow-node.js";
import { FlowTrigger } from "./flow-trigger.js";
import { TriggerBinding } from "./trigger-binding.js";
/**
 * Root flow program.
 */
export class FlowProgram {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsFlowProgram(bb, obj) {
    return (obj || new FlowProgram()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsFlowProgram(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new FlowProgram()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static bufferHasIdentifier(bb) {
    return bb.__has_identifier("FLOW");
  }
  programId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  name(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  version(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  nodes(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset
      ? (obj || new FlowNode()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  nodesLength() {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  edges(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset
      ? (obj || new FlowEdge()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  edgesLength() {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  triggers(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset
      ? (obj || new FlowTrigger()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  triggersLength() {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  triggerBindings(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 16);
    return offset
      ? (obj || new TriggerBinding()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  triggerBindingsLength() {
    const offset = this.bb.__offset(this.bb_pos, 16);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  requiredPlugins(index, optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 18);
    return offset
      ? this.bb.__string(
          this.bb.__vector(this.bb_pos + offset) + index * 4,
          optionalEncoding,
        )
      : null;
  }
  requiredPluginsLength() {
    const offset = this.bb.__offset(this.bb_pos, 18);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  description(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 20);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  static startFlowProgram(builder) {
    builder.startObject(9);
  }
  static addProgramId(builder, programIdOffset) {
    builder.addFieldOffset(0, programIdOffset, 0);
  }
  static addName(builder, nameOffset) {
    builder.addFieldOffset(1, nameOffset, 0);
  }
  static addVersion(builder, versionOffset) {
    builder.addFieldOffset(2, versionOffset, 0);
  }
  static addNodes(builder, nodesOffset) {
    builder.addFieldOffset(3, nodesOffset, 0);
  }
  static createNodesVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startNodesVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addEdges(builder, edgesOffset) {
    builder.addFieldOffset(4, edgesOffset, 0);
  }
  static createEdgesVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startEdgesVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addTriggers(builder, triggersOffset) {
    builder.addFieldOffset(5, triggersOffset, 0);
  }
  static createTriggersVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startTriggersVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addTriggerBindings(builder, triggerBindingsOffset) {
    builder.addFieldOffset(6, triggerBindingsOffset, 0);
  }
  static createTriggerBindingsVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startTriggerBindingsVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addRequiredPlugins(builder, requiredPluginsOffset) {
    builder.addFieldOffset(7, requiredPluginsOffset, 0);
  }
  static createRequiredPluginsVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startRequiredPluginsVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addDescription(builder, descriptionOffset) {
    builder.addFieldOffset(8, descriptionOffset, 0);
  }
  static endFlowProgram(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // program_id
    builder.requiredField(offset, 6); // name
    return offset;
  }
  static finishFlowProgramBuffer(builder, offset) {
    builder.finish(offset, "FLOW");
  }
  static finishSizePrefixedFlowProgramBuffer(builder, offset) {
    builder.finish(offset, "FLOW", true);
  }
  static createFlowProgram(
    builder,
    programIdOffset,
    nameOffset,
    versionOffset,
    nodesOffset,
    edgesOffset,
    triggersOffset,
    triggerBindingsOffset,
    requiredPluginsOffset,
    descriptionOffset,
  ) {
    FlowProgram.startFlowProgram(builder);
    FlowProgram.addProgramId(builder, programIdOffset);
    FlowProgram.addName(builder, nameOffset);
    FlowProgram.addVersion(builder, versionOffset);
    FlowProgram.addNodes(builder, nodesOffset);
    FlowProgram.addEdges(builder, edgesOffset);
    FlowProgram.addTriggers(builder, triggersOffset);
    FlowProgram.addTriggerBindings(builder, triggerBindingsOffset);
    FlowProgram.addRequiredPlugins(builder, requiredPluginsOffset);
    FlowProgram.addDescription(builder, descriptionOffset);
    return FlowProgram.endFlowProgram(builder);
  }
  unpack() {
    return new FlowProgramT(
      this.programId(),
      this.name(),
      this.version(),
      this.bb.createObjList(this.nodes.bind(this), this.nodesLength()),
      this.bb.createObjList(this.edges.bind(this), this.edgesLength()),
      this.bb.createObjList(this.triggers.bind(this), this.triggersLength()),
      this.bb.createObjList(
        this.triggerBindings.bind(this),
        this.triggerBindingsLength(),
      ),
      this.bb.createScalarList(
        this.requiredPlugins.bind(this),
        this.requiredPluginsLength(),
      ),
      this.description(),
    );
  }
  unpackTo(_o) {
    _o.programId = this.programId();
    _o.name = this.name();
    _o.version = this.version();
    _o.nodes = this.bb.createObjList(this.nodes.bind(this), this.nodesLength());
    _o.edges = this.bb.createObjList(this.edges.bind(this), this.edgesLength());
    _o.triggers = this.bb.createObjList(
      this.triggers.bind(this),
      this.triggersLength(),
    );
    _o.triggerBindings = this.bb.createObjList(
      this.triggerBindings.bind(this),
      this.triggerBindingsLength(),
    );
    _o.requiredPlugins = this.bb.createScalarList(
      this.requiredPlugins.bind(this),
      this.requiredPluginsLength(),
    );
    _o.description = this.description();
  }
}
export class FlowProgramT {
  constructor(
    programId = null,
    name = null,
    version = null,
    nodes = [],
    edges = [],
    triggers = [],
    triggerBindings = [],
    requiredPlugins = [],
    description = null,
  ) {
    this.programId = programId;
    this.name = name;
    this.version = version;
    this.nodes = nodes;
    this.edges = edges;
    this.triggers = triggers;
    this.triggerBindings = triggerBindings;
    this.requiredPlugins = requiredPlugins;
    this.description = description;
  }
  pack(builder) {
    const programId =
      this.programId !== null ? builder.createString(this.programId) : 0;
    const name = this.name !== null ? builder.createString(this.name) : 0;
    const version =
      this.version !== null ? builder.createString(this.version) : 0;
    const nodes = FlowProgram.createNodesVector(
      builder,
      builder.createObjectOffsetList(this.nodes),
    );
    const edges = FlowProgram.createEdgesVector(
      builder,
      builder.createObjectOffsetList(this.edges),
    );
    const triggers = FlowProgram.createTriggersVector(
      builder,
      builder.createObjectOffsetList(this.triggers),
    );
    const triggerBindings = FlowProgram.createTriggerBindingsVector(
      builder,
      builder.createObjectOffsetList(this.triggerBindings),
    );
    const requiredPlugins = FlowProgram.createRequiredPluginsVector(
      builder,
      builder.createObjectOffsetList(this.requiredPlugins),
    );
    const description =
      this.description !== null ? builder.createString(this.description) : 0;
    return FlowProgram.createFlowProgram(
      builder,
      programId,
      name,
      version,
      nodes,
      edges,
      triggers,
      triggerBindings,
      requiredPlugins,
      description,
    );
  }
}
//# sourceMappingURL=flow-program.js.map
