// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from "flatbuffers";

import { BackpressurePolicy } from "./backpressure-policy.js";

/**
 * Binds a trigger to a node input port.
 */
export class TriggerBinding implements flatbuffers.IUnpackableObject<TriggerBindingT> {
  bb: flatbuffers.ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: flatbuffers.ByteBuffer): TriggerBinding {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsTriggerBinding(
    bb: flatbuffers.ByteBuffer,
    obj?: TriggerBinding,
  ): TriggerBinding {
    return (obj || new TriggerBinding()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  static getSizePrefixedRootAsTriggerBinding(
    bb: flatbuffers.ByteBuffer,
    obj?: TriggerBinding,
  ): TriggerBinding {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new TriggerBinding()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  triggerId(): string;
  triggerId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  triggerId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  targetNodeId(): string;
  targetNodeId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  targetNodeId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  targetPortId(): string;
  targetPortId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  targetPortId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  backpressurePolicy(): BackpressurePolicy {
    const offset = this.bb!.__offset(this.bb_pos, 10);
    return offset
      ? this.bb!.readUint8(this.bb_pos + offset)
      : BackpressurePolicy.QUEUE;
  }

  queueDepth(): number {
    const offset = this.bb!.__offset(this.bb_pos, 12);
    return offset ? this.bb!.readUint32(this.bb_pos + offset) : 1;
  }

  static startTriggerBinding(builder: flatbuffers.Builder) {
    builder.startObject(5);
  }

  static addTriggerId(
    builder: flatbuffers.Builder,
    triggerIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(0, triggerIdOffset, 0);
  }

  static addTargetNodeId(
    builder: flatbuffers.Builder,
    targetNodeIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(1, targetNodeIdOffset, 0);
  }

  static addTargetPortId(
    builder: flatbuffers.Builder,
    targetPortIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(2, targetPortIdOffset, 0);
  }

  static addBackpressurePolicy(
    builder: flatbuffers.Builder,
    backpressurePolicy: BackpressurePolicy,
  ) {
    builder.addFieldInt8(3, backpressurePolicy, BackpressurePolicy.QUEUE);
  }

  static addQueueDepth(builder: flatbuffers.Builder, queueDepth: number) {
    builder.addFieldInt32(4, queueDepth, 1);
  }

  static endTriggerBinding(builder: flatbuffers.Builder): flatbuffers.Offset {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // trigger_id
    builder.requiredField(offset, 6); // target_node_id
    builder.requiredField(offset, 8); // target_port_id
    return offset;
  }

  static createTriggerBinding(
    builder: flatbuffers.Builder,
    triggerIdOffset: flatbuffers.Offset,
    targetNodeIdOffset: flatbuffers.Offset,
    targetPortIdOffset: flatbuffers.Offset,
    backpressurePolicy: BackpressurePolicy,
    queueDepth: number,
  ): flatbuffers.Offset {
    TriggerBinding.startTriggerBinding(builder);
    TriggerBinding.addTriggerId(builder, triggerIdOffset);
    TriggerBinding.addTargetNodeId(builder, targetNodeIdOffset);
    TriggerBinding.addTargetPortId(builder, targetPortIdOffset);
    TriggerBinding.addBackpressurePolicy(builder, backpressurePolicy);
    TriggerBinding.addQueueDepth(builder, queueDepth);
    return TriggerBinding.endTriggerBinding(builder);
  }

  unpack(): TriggerBindingT {
    return new TriggerBindingT(
      this.triggerId(),
      this.targetNodeId(),
      this.targetPortId(),
      this.backpressurePolicy(),
      this.queueDepth(),
    );
  }

  unpackTo(_o: TriggerBindingT): void {
    _o.triggerId = this.triggerId();
    _o.targetNodeId = this.targetNodeId();
    _o.targetPortId = this.targetPortId();
    _o.backpressurePolicy = this.backpressurePolicy();
    _o.queueDepth = this.queueDepth();
  }
}

export class TriggerBindingT implements flatbuffers.IGeneratedObject {
  constructor(
    public triggerId: string | Uint8Array | null = null,
    public targetNodeId: string | Uint8Array | null = null,
    public targetPortId: string | Uint8Array | null = null,
    public backpressurePolicy: BackpressurePolicy = BackpressurePolicy.QUEUE,
    public queueDepth: number = 1,
  ) {}

  pack(builder: flatbuffers.Builder): flatbuffers.Offset {
    const triggerId =
      this.triggerId !== null ? builder.createString(this.triggerId!) : 0;
    const targetNodeId =
      this.targetNodeId !== null ? builder.createString(this.targetNodeId!) : 0;
    const targetPortId =
      this.targetPortId !== null ? builder.createString(this.targetPortId!) : 0;

    return TriggerBinding.createTriggerBinding(
      builder,
      triggerId,
      targetNodeId,
      targetPortId,
      this.backpressurePolicy,
      this.queueDepth,
    );
  }
}
