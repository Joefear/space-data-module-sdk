// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { PluginVersionBinding } from "./plugin-version-binding.js";
import { SchemaHashBinding } from "./schema-hash-binding.js";
/**
 * Bundle manifest for compiled flow deployment.
 */
export class FlowBundleManifest {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsFlowBundleManifest(bb, obj) {
    return (obj || new FlowBundleManifest()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsFlowBundleManifest(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new FlowBundleManifest()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  bundleId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  programId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  graphHash(index) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index)
      : 0;
  }
  graphHashLength() {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  graphHashArray() {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? new Uint8Array(
          this.bb.bytes().buffer,
          this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset),
          this.bb.__vector_len(this.bb_pos + offset),
        )
      : null;
  }
  pluginVersions(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset
      ? (obj || new PluginVersionBinding()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  pluginVersionsLength() {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  schemaHashes(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset
      ? (obj || new SchemaHashBinding()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  schemaHashesLength() {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  requiredCapabilities(index) {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset
      ? this.bb.readUint16(this.bb.__vector(this.bb_pos + offset) + index * 2)
      : null;
  }
  requiredCapabilitiesLength() {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  requiredCapabilitiesArray() {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset
      ? new Uint16Array(
          this.bb.bytes().buffer,
          this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset),
          this.bb.__vector_len(this.bb_pos + offset),
        )
      : null;
  }
  abiVersion() {
    const offset = this.bb.__offset(this.bb_pos, 16);
    return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
  }
  static startFlowBundleManifest(builder) {
    builder.startObject(7);
  }
  static addBundleId(builder, bundleIdOffset) {
    builder.addFieldOffset(0, bundleIdOffset, 0);
  }
  static addProgramId(builder, programIdOffset) {
    builder.addFieldOffset(1, programIdOffset, 0);
  }
  static addGraphHash(builder, graphHashOffset) {
    builder.addFieldOffset(2, graphHashOffset, 0);
  }
  static createGraphHashVector(builder, data) {
    builder.startVector(1, data.length, 1);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addInt8(data[i]);
    }
    return builder.endVector();
  }
  static startGraphHashVector(builder, numElems) {
    builder.startVector(1, numElems, 1);
  }
  static addPluginVersions(builder, pluginVersionsOffset) {
    builder.addFieldOffset(3, pluginVersionsOffset, 0);
  }
  static createPluginVersionsVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startPluginVersionsVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addSchemaHashes(builder, schemaHashesOffset) {
    builder.addFieldOffset(4, schemaHashesOffset, 0);
  }
  static createSchemaHashesVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startSchemaHashesVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addRequiredCapabilities(builder, requiredCapabilitiesOffset) {
    builder.addFieldOffset(5, requiredCapabilitiesOffset, 0);
  }
  static createRequiredCapabilitiesVector(builder, data) {
    builder.startVector(2, data.length, 2);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addInt16(data[i]);
    }
    return builder.endVector();
  }
  static startRequiredCapabilitiesVector(builder, numElems) {
    builder.startVector(2, numElems, 2);
  }
  static addAbiVersion(builder, abiVersion) {
    builder.addFieldInt32(6, abiVersion, 1);
  }
  static endFlowBundleManifest(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // bundle_id
    builder.requiredField(offset, 6); // program_id
    return offset;
  }
  static createFlowBundleManifest(
    builder,
    bundleIdOffset,
    programIdOffset,
    graphHashOffset,
    pluginVersionsOffset,
    schemaHashesOffset,
    requiredCapabilitiesOffset,
    abiVersion,
  ) {
    FlowBundleManifest.startFlowBundleManifest(builder);
    FlowBundleManifest.addBundleId(builder, bundleIdOffset);
    FlowBundleManifest.addProgramId(builder, programIdOffset);
    FlowBundleManifest.addGraphHash(builder, graphHashOffset);
    FlowBundleManifest.addPluginVersions(builder, pluginVersionsOffset);
    FlowBundleManifest.addSchemaHashes(builder, schemaHashesOffset);
    FlowBundleManifest.addRequiredCapabilities(
      builder,
      requiredCapabilitiesOffset,
    );
    FlowBundleManifest.addAbiVersion(builder, abiVersion);
    return FlowBundleManifest.endFlowBundleManifest(builder);
  }
  unpack() {
    return new FlowBundleManifestT(
      this.bundleId(),
      this.programId(),
      this.bb.createScalarList(
        this.graphHash.bind(this),
        this.graphHashLength(),
      ),
      this.bb.createObjList(
        this.pluginVersions.bind(this),
        this.pluginVersionsLength(),
      ),
      this.bb.createObjList(
        this.schemaHashes.bind(this),
        this.schemaHashesLength(),
      ),
      this.bb.createScalarList(
        this.requiredCapabilities.bind(this),
        this.requiredCapabilitiesLength(),
      ),
      this.abiVersion(),
    );
  }
  unpackTo(_o) {
    _o.bundleId = this.bundleId();
    _o.programId = this.programId();
    _o.graphHash = this.bb.createScalarList(
      this.graphHash.bind(this),
      this.graphHashLength(),
    );
    _o.pluginVersions = this.bb.createObjList(
      this.pluginVersions.bind(this),
      this.pluginVersionsLength(),
    );
    _o.schemaHashes = this.bb.createObjList(
      this.schemaHashes.bind(this),
      this.schemaHashesLength(),
    );
    _o.requiredCapabilities = this.bb.createScalarList(
      this.requiredCapabilities.bind(this),
      this.requiredCapabilitiesLength(),
    );
    _o.abiVersion = this.abiVersion();
  }
}
export class FlowBundleManifestT {
  constructor(
    bundleId = null,
    programId = null,
    graphHash = [],
    pluginVersions = [],
    schemaHashes = [],
    requiredCapabilities = [],
    abiVersion = 1,
  ) {
    this.bundleId = bundleId;
    this.programId = programId;
    this.graphHash = graphHash;
    this.pluginVersions = pluginVersions;
    this.schemaHashes = schemaHashes;
    this.requiredCapabilities = requiredCapabilities;
    this.abiVersion = abiVersion;
  }
  pack(builder) {
    const bundleId =
      this.bundleId !== null ? builder.createString(this.bundleId) : 0;
    const programId =
      this.programId !== null ? builder.createString(this.programId) : 0;
    const graphHash = FlowBundleManifest.createGraphHashVector(
      builder,
      this.graphHash,
    );
    const pluginVersions = FlowBundleManifest.createPluginVersionsVector(
      builder,
      builder.createObjectOffsetList(this.pluginVersions),
    );
    const schemaHashes = FlowBundleManifest.createSchemaHashesVector(
      builder,
      builder.createObjectOffsetList(this.schemaHashes),
    );
    const requiredCapabilities =
      FlowBundleManifest.createRequiredCapabilitiesVector(
        builder,
        this.requiredCapabilities,
      );
    return FlowBundleManifest.createFlowBundleManifest(
      builder,
      bundleId,
      programId,
      graphHash,
      pluginVersions,
      schemaHashes,
      requiredCapabilities,
      this.abiVersion,
    );
  }
}
//# sourceMappingURL=flow-bundle-manifest.js.map
