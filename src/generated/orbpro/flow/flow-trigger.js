// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { TriggerKind } from "./trigger-kind.js";
import { FlatBufferTypeRef } from "../stream/flat-buffer-type-ref.js";
/**
 * One runtime trigger definition.
 */
export class FlowTrigger {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsFlowTrigger(bb, obj) {
    return (obj || new FlowTrigger()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsFlowTrigger(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new FlowTrigger()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  triggerId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  kind() {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return offset
      ? this.bb.readUint8(this.bb_pos + offset)
      : TriggerKind.MANUAL;
  }
  source(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  protocolId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  defaultIntervalMs() {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt("0");
  }
  acceptedTypes(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset
      ? (obj || new FlatBufferTypeRef()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  acceptedTypesLength() {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  description(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 16);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  static startFlowTrigger(builder) {
    builder.startObject(7);
  }
  static addTriggerId(builder, triggerIdOffset) {
    builder.addFieldOffset(0, triggerIdOffset, 0);
  }
  static addKind(builder, kind) {
    builder.addFieldInt8(1, kind, TriggerKind.MANUAL);
  }
  static addSource(builder, sourceOffset) {
    builder.addFieldOffset(2, sourceOffset, 0);
  }
  static addProtocolId(builder, protocolIdOffset) {
    builder.addFieldOffset(3, protocolIdOffset, 0);
  }
  static addDefaultIntervalMs(builder, defaultIntervalMs) {
    builder.addFieldInt64(4, defaultIntervalMs, BigInt("0"));
  }
  static addAcceptedTypes(builder, acceptedTypesOffset) {
    builder.addFieldOffset(5, acceptedTypesOffset, 0);
  }
  static createAcceptedTypesVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startAcceptedTypesVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addDescription(builder, descriptionOffset) {
    builder.addFieldOffset(6, descriptionOffset, 0);
  }
  static endFlowTrigger(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // trigger_id
    return offset;
  }
  static createFlowTrigger(
    builder,
    triggerIdOffset,
    kind,
    sourceOffset,
    protocolIdOffset,
    defaultIntervalMs,
    acceptedTypesOffset,
    descriptionOffset,
  ) {
    FlowTrigger.startFlowTrigger(builder);
    FlowTrigger.addTriggerId(builder, triggerIdOffset);
    FlowTrigger.addKind(builder, kind);
    FlowTrigger.addSource(builder, sourceOffset);
    FlowTrigger.addProtocolId(builder, protocolIdOffset);
    FlowTrigger.addDefaultIntervalMs(builder, defaultIntervalMs);
    FlowTrigger.addAcceptedTypes(builder, acceptedTypesOffset);
    FlowTrigger.addDescription(builder, descriptionOffset);
    return FlowTrigger.endFlowTrigger(builder);
  }
  unpack() {
    return new FlowTriggerT(
      this.triggerId(),
      this.kind(),
      this.source(),
      this.protocolId(),
      this.defaultIntervalMs(),
      this.bb.createObjList(
        this.acceptedTypes.bind(this),
        this.acceptedTypesLength(),
      ),
      this.description(),
    );
  }
  unpackTo(_o) {
    _o.triggerId = this.triggerId();
    _o.kind = this.kind();
    _o.source = this.source();
    _o.protocolId = this.protocolId();
    _o.defaultIntervalMs = this.defaultIntervalMs();
    _o.acceptedTypes = this.bb.createObjList(
      this.acceptedTypes.bind(this),
      this.acceptedTypesLength(),
    );
    _o.description = this.description();
  }
}
export class FlowTriggerT {
  constructor(
    triggerId = null,
    kind = TriggerKind.MANUAL,
    source = null,
    protocolId = null,
    defaultIntervalMs = BigInt("0"),
    acceptedTypes = [],
    description = null,
  ) {
    this.triggerId = triggerId;
    this.kind = kind;
    this.source = source;
    this.protocolId = protocolId;
    this.defaultIntervalMs = defaultIntervalMs;
    this.acceptedTypes = acceptedTypes;
    this.description = description;
  }
  pack(builder) {
    const triggerId =
      this.triggerId !== null ? builder.createString(this.triggerId) : 0;
    const source = this.source !== null ? builder.createString(this.source) : 0;
    const protocolId =
      this.protocolId !== null ? builder.createString(this.protocolId) : 0;
    const acceptedTypes = FlowTrigger.createAcceptedTypesVector(
      builder,
      builder.createObjectOffsetList(this.acceptedTypes),
    );
    const description =
      this.description !== null ? builder.createString(this.description) : 0;
    return FlowTrigger.createFlowTrigger(
      builder,
      triggerId,
      this.kind,
      source,
      protocolId,
      this.defaultIntervalMs,
      acceptedTypes,
      description,
    );
  }
}
//# sourceMappingURL=flow-trigger.js.map
