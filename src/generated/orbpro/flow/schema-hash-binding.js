// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
/**
 * Pins a schema hash into a deployable bundle.
 */
export class SchemaHashBinding {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsSchemaHashBinding(bb, obj) {
    return (obj || new SchemaHashBinding()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsSchemaHashBinding(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new SchemaHashBinding()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  schemaName(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  fileIdentifier(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return offset
      ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
      : null;
  }
  schemaHash(index) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index)
      : 0;
  }
  schemaHashLength() {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  schemaHashArray() {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return offset
      ? new Uint8Array(
          this.bb.bytes().buffer,
          this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset),
          this.bb.__vector_len(this.bb_pos + offset),
        )
      : null;
  }
  static startSchemaHashBinding(builder) {
    builder.startObject(3);
  }
  static addSchemaName(builder, schemaNameOffset) {
    builder.addFieldOffset(0, schemaNameOffset, 0);
  }
  static addFileIdentifier(builder, fileIdentifierOffset) {
    builder.addFieldOffset(1, fileIdentifierOffset, 0);
  }
  static addSchemaHash(builder, schemaHashOffset) {
    builder.addFieldOffset(2, schemaHashOffset, 0);
  }
  static createSchemaHashVector(builder, data) {
    builder.startVector(1, data.length, 1);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addInt8(data[i]);
    }
    return builder.endVector();
  }
  static startSchemaHashVector(builder, numElems) {
    builder.startVector(1, numElems, 1);
  }
  static endSchemaHashBinding(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // schema_name
    return offset;
  }
  static createSchemaHashBinding(
    builder,
    schemaNameOffset,
    fileIdentifierOffset,
    schemaHashOffset,
  ) {
    SchemaHashBinding.startSchemaHashBinding(builder);
    SchemaHashBinding.addSchemaName(builder, schemaNameOffset);
    SchemaHashBinding.addFileIdentifier(builder, fileIdentifierOffset);
    SchemaHashBinding.addSchemaHash(builder, schemaHashOffset);
    return SchemaHashBinding.endSchemaHashBinding(builder);
  }
  unpack() {
    return new SchemaHashBindingT(
      this.schemaName(),
      this.fileIdentifier(),
      this.bb.createScalarList(
        this.schemaHash.bind(this),
        this.schemaHashLength(),
      ),
    );
  }
  unpackTo(_o) {
    _o.schemaName = this.schemaName();
    _o.fileIdentifier = this.fileIdentifier();
    _o.schemaHash = this.bb.createScalarList(
      this.schemaHash.bind(this),
      this.schemaHashLength(),
    );
  }
}
export class SchemaHashBindingT {
  constructor(schemaName = null, fileIdentifier = null, schemaHash = []) {
    this.schemaName = schemaName;
    this.fileIdentifier = fileIdentifier;
    this.schemaHash = schemaHash;
  }
  pack(builder) {
    const schemaName =
      this.schemaName !== null ? builder.createString(this.schemaName) : 0;
    const fileIdentifier =
      this.fileIdentifier !== null
        ? builder.createString(this.fileIdentifier)
        : 0;
    const schemaHash = SchemaHashBinding.createSchemaHashVector(
      builder,
      this.schemaHash,
    );
    return SchemaHashBinding.createSchemaHashBinding(
      builder,
      schemaName,
      fileIdentifier,
      schemaHash,
    );
  }
}
//# sourceMappingURL=schema-hash-binding.js.map
