// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { BackpressurePolicy } from "./backpressure-policy.js";
/**
 * Binds a trigger to a node input port.
 */
export class TriggerBinding {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsTriggerBinding(bb, obj) {
    return (obj || new TriggerBinding()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsTriggerBinding(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new TriggerBinding()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  triggerId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  targetNodeId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  targetPortId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  backpressurePolicy() {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset
      ? this.bb.readUint8(this.bb_pos + offset)
      : BackpressurePolicy.QUEUE;
  }
  queueDepth() {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
  }
  static startTriggerBinding(builder) {
    builder.startObject(5);
  }
  static addTriggerId(builder, triggerIdOffset) {
    builder.addFieldOffset(0, triggerIdOffset, 0);
  }
  static addTargetNodeId(builder, targetNodeIdOffset) {
    builder.addFieldOffset(1, targetNodeIdOffset, 0);
  }
  static addTargetPortId(builder, targetPortIdOffset) {
    builder.addFieldOffset(2, targetPortIdOffset, 0);
  }
  static addBackpressurePolicy(builder, backpressurePolicy) {
    builder.addFieldInt8(3, backpressurePolicy, BackpressurePolicy.QUEUE);
  }
  static addQueueDepth(builder, queueDepth) {
    builder.addFieldInt32(4, queueDepth, 1);
  }
  static endTriggerBinding(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // trigger_id
    builder.requiredField(offset, 6); // target_node_id
    builder.requiredField(offset, 8); // target_port_id
    return offset;
  }
  static createTriggerBinding(
    builder,
    triggerIdOffset,
    targetNodeIdOffset,
    targetPortIdOffset,
    backpressurePolicy,
    queueDepth,
  ) {
    TriggerBinding.startTriggerBinding(builder);
    TriggerBinding.addTriggerId(builder, triggerIdOffset);
    TriggerBinding.addTargetNodeId(builder, targetNodeIdOffset);
    TriggerBinding.addTargetPortId(builder, targetPortIdOffset);
    TriggerBinding.addBackpressurePolicy(builder, backpressurePolicy);
    TriggerBinding.addQueueDepth(builder, queueDepth);
    return TriggerBinding.endTriggerBinding(builder);
  }
  unpack() {
    return new TriggerBindingT(
      this.triggerId(),
      this.targetNodeId(),
      this.targetPortId(),
      this.backpressurePolicy(),
      this.queueDepth(),
    );
  }
  unpackTo(_o) {
    _o.triggerId = this.triggerId();
    _o.targetNodeId = this.targetNodeId();
    _o.targetPortId = this.targetPortId();
    _o.backpressurePolicy = this.backpressurePolicy();
    _o.queueDepth = this.queueDepth();
  }
}
export class TriggerBindingT {
  constructor(
    triggerId = null,
    targetNodeId = null,
    targetPortId = null,
    backpressurePolicy = BackpressurePolicy.QUEUE,
    queueDepth = 1,
  ) {
    this.triggerId = triggerId;
    this.targetNodeId = targetNodeId;
    this.targetPortId = targetPortId;
    this.backpressurePolicy = backpressurePolicy;
    this.queueDepth = queueDepth;
  }
  pack(builder) {
    const triggerId =
      this.triggerId !== null ? builder.createString(this.triggerId) : 0;
    const targetNodeId =
      this.targetNodeId !== null ? builder.createString(this.targetNodeId) : 0;
    const targetPortId =
      this.targetPortId !== null ? builder.createString(this.targetPortId) : 0;
    return TriggerBinding.createTriggerBinding(
      builder,
      triggerId,
      targetNodeId,
      targetPortId,
      this.backpressurePolicy,
      this.queueDepth,
    );
  }
}
//# sourceMappingURL=trigger-binding.js.map
