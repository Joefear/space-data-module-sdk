// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { BackpressurePolicy } from "./backpressure-policy.js";
import { FlatBufferTypeRef } from "../stream/flat-buffer-type-ref.js";
/**
 * One directed edge between node ports.
 */
export class FlowEdge {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsFlowEdge(bb, obj) {
    return (obj || new FlowEdge()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsFlowEdge(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new FlowEdge()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  edgeId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  fromNodeId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  fromPortId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  toNodeId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  toPortId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  acceptedTypes(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset
      ? (obj || new FlatBufferTypeRef()).__init(
          this.bb.__indirect(
            this.bb.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb,
        )
      : null;
  }
  acceptedTypesLength() {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  backpressurePolicy() {
    const offset = this.bb.__offset(this.bb_pos, 16);
    return offset
      ? this.bb.readUint8(this.bb_pos + offset)
      : BackpressurePolicy.QUEUE;
  }
  queueDepth() {
    const offset = this.bb.__offset(this.bb_pos, 18);
    return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
  }
  static startFlowEdge(builder) {
    builder.startObject(8);
  }
  static addEdgeId(builder, edgeIdOffset) {
    builder.addFieldOffset(0, edgeIdOffset, 0);
  }
  static addFromNodeId(builder, fromNodeIdOffset) {
    builder.addFieldOffset(1, fromNodeIdOffset, 0);
  }
  static addFromPortId(builder, fromPortIdOffset) {
    builder.addFieldOffset(2, fromPortIdOffset, 0);
  }
  static addToNodeId(builder, toNodeIdOffset) {
    builder.addFieldOffset(3, toNodeIdOffset, 0);
  }
  static addToPortId(builder, toPortIdOffset) {
    builder.addFieldOffset(4, toPortIdOffset, 0);
  }
  static addAcceptedTypes(builder, acceptedTypesOffset) {
    builder.addFieldOffset(5, acceptedTypesOffset, 0);
  }
  static createAcceptedTypesVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startAcceptedTypesVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static addBackpressurePolicy(builder, backpressurePolicy) {
    builder.addFieldInt8(6, backpressurePolicy, BackpressurePolicy.QUEUE);
  }
  static addQueueDepth(builder, queueDepth) {
    builder.addFieldInt32(7, queueDepth, 1);
  }
  static endFlowEdge(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // edge_id
    builder.requiredField(offset, 6); // from_node_id
    builder.requiredField(offset, 8); // from_port_id
    builder.requiredField(offset, 10); // to_node_id
    builder.requiredField(offset, 12); // to_port_id
    return offset;
  }
  static createFlowEdge(
    builder,
    edgeIdOffset,
    fromNodeIdOffset,
    fromPortIdOffset,
    toNodeIdOffset,
    toPortIdOffset,
    acceptedTypesOffset,
    backpressurePolicy,
    queueDepth,
  ) {
    FlowEdge.startFlowEdge(builder);
    FlowEdge.addEdgeId(builder, edgeIdOffset);
    FlowEdge.addFromNodeId(builder, fromNodeIdOffset);
    FlowEdge.addFromPortId(builder, fromPortIdOffset);
    FlowEdge.addToNodeId(builder, toNodeIdOffset);
    FlowEdge.addToPortId(builder, toPortIdOffset);
    FlowEdge.addAcceptedTypes(builder, acceptedTypesOffset);
    FlowEdge.addBackpressurePolicy(builder, backpressurePolicy);
    FlowEdge.addQueueDepth(builder, queueDepth);
    return FlowEdge.endFlowEdge(builder);
  }
  unpack() {
    return new FlowEdgeT(
      this.edgeId(),
      this.fromNodeId(),
      this.fromPortId(),
      this.toNodeId(),
      this.toPortId(),
      this.bb.createObjList(
        this.acceptedTypes.bind(this),
        this.acceptedTypesLength(),
      ),
      this.backpressurePolicy(),
      this.queueDepth(),
    );
  }
  unpackTo(_o) {
    _o.edgeId = this.edgeId();
    _o.fromNodeId = this.fromNodeId();
    _o.fromPortId = this.fromPortId();
    _o.toNodeId = this.toNodeId();
    _o.toPortId = this.toPortId();
    _o.acceptedTypes = this.bb.createObjList(
      this.acceptedTypes.bind(this),
      this.acceptedTypesLength(),
    );
    _o.backpressurePolicy = this.backpressurePolicy();
    _o.queueDepth = this.queueDepth();
  }
}
export class FlowEdgeT {
  constructor(
    edgeId = null,
    fromNodeId = null,
    fromPortId = null,
    toNodeId = null,
    toPortId = null,
    acceptedTypes = [],
    backpressurePolicy = BackpressurePolicy.QUEUE,
    queueDepth = 1,
  ) {
    this.edgeId = edgeId;
    this.fromNodeId = fromNodeId;
    this.fromPortId = fromPortId;
    this.toNodeId = toNodeId;
    this.toPortId = toPortId;
    this.acceptedTypes = acceptedTypes;
    this.backpressurePolicy = backpressurePolicy;
    this.queueDepth = queueDepth;
  }
  pack(builder) {
    const edgeId = this.edgeId !== null ? builder.createString(this.edgeId) : 0;
    const fromNodeId =
      this.fromNodeId !== null ? builder.createString(this.fromNodeId) : 0;
    const fromPortId =
      this.fromPortId !== null ? builder.createString(this.fromPortId) : 0;
    const toNodeId =
      this.toNodeId !== null ? builder.createString(this.toNodeId) : 0;
    const toPortId =
      this.toPortId !== null ? builder.createString(this.toPortId) : 0;
    const acceptedTypes = FlowEdge.createAcceptedTypesVector(
      builder,
      builder.createObjectOffsetList(this.acceptedTypes),
    );
    return FlowEdge.createFlowEdge(
      builder,
      edgeId,
      fromNodeId,
      fromPortId,
      toNodeId,
      toPortId,
      acceptedTypes,
      this.backpressurePolicy,
      this.queueDepth,
    );
  }
}
//# sourceMappingURL=flow-edge.js.map
