// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from "flatbuffers";

import { NodeKind } from "./node-kind.js";
import { DrainPolicy } from "../manifest/drain-policy.js";

/**
 * One plugin-backed flow node.
 */
export class FlowNode implements flatbuffers.IUnpackableObject<FlowNodeT> {
  bb: flatbuffers.ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: flatbuffers.ByteBuffer): FlowNode {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsFlowNode(
    bb: flatbuffers.ByteBuffer,
    obj?: FlowNode,
  ): FlowNode {
    return (obj || new FlowNode()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  static getSizePrefixedRootAsFlowNode(
    bb: flatbuffers.ByteBuffer,
    obj?: FlowNode,
  ): FlowNode {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new FlowNode()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  nodeId(): string;
  nodeId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  nodeId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  pluginId(): string;
  pluginId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  pluginId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  methodId(): string;
  methodId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  methodId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  kind(): NodeKind {
    const offset = this.bb!.__offset(this.bb_pos, 10);
    return offset
      ? this.bb!.readUint8(this.bb_pos + offset)
      : NodeKind.TRANSFORM;
  }

  drainPolicy(): DrainPolicy {
    const offset = this.bb!.__offset(this.bb_pos, 12);
    return offset
      ? this.bb!.readUint8(this.bb_pos + offset)
      : DrainPolicy.DRAIN_UNTIL_YIELD;
  }

  timeSliceMicros(): number {
    const offset = this.bb!.__offset(this.bb_pos, 14);
    return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
  }

  static startFlowNode(builder: flatbuffers.Builder) {
    builder.startObject(6);
  }

  static addNodeId(
    builder: flatbuffers.Builder,
    nodeIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(0, nodeIdOffset, 0);
  }

  static addPluginId(
    builder: flatbuffers.Builder,
    pluginIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(1, pluginIdOffset, 0);
  }

  static addMethodId(
    builder: flatbuffers.Builder,
    methodIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(2, methodIdOffset, 0);
  }

  static addKind(builder: flatbuffers.Builder, kind: NodeKind) {
    builder.addFieldInt8(3, kind, NodeKind.TRANSFORM);
  }

  static addDrainPolicy(
    builder: flatbuffers.Builder,
    drainPolicy: DrainPolicy,
  ) {
    builder.addFieldInt8(4, drainPolicy, DrainPolicy.DRAIN_UNTIL_YIELD);
  }

  static addTimeSliceMicros(
    builder: flatbuffers.Builder,
    timeSliceMicros: number,
  ) {
    builder.addFieldInt32(5, timeSliceMicros, 0);
  }

  static endFlowNode(builder: flatbuffers.Builder): flatbuffers.Offset {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // node_id
    builder.requiredField(offset, 6); // plugin_id
    builder.requiredField(offset, 8); // method_id
    return offset;
  }

  static createFlowNode(
    builder: flatbuffers.Builder,
    nodeIdOffset: flatbuffers.Offset,
    pluginIdOffset: flatbuffers.Offset,
    methodIdOffset: flatbuffers.Offset,
    kind: NodeKind,
    drainPolicy: DrainPolicy,
    timeSliceMicros: number,
  ): flatbuffers.Offset {
    FlowNode.startFlowNode(builder);
    FlowNode.addNodeId(builder, nodeIdOffset);
    FlowNode.addPluginId(builder, pluginIdOffset);
    FlowNode.addMethodId(builder, methodIdOffset);
    FlowNode.addKind(builder, kind);
    FlowNode.addDrainPolicy(builder, drainPolicy);
    FlowNode.addTimeSliceMicros(builder, timeSliceMicros);
    return FlowNode.endFlowNode(builder);
  }

  unpack(): FlowNodeT {
    return new FlowNodeT(
      this.nodeId(),
      this.pluginId(),
      this.methodId(),
      this.kind(),
      this.drainPolicy(),
      this.timeSliceMicros(),
    );
  }

  unpackTo(_o: FlowNodeT): void {
    _o.nodeId = this.nodeId();
    _o.pluginId = this.pluginId();
    _o.methodId = this.methodId();
    _o.kind = this.kind();
    _o.drainPolicy = this.drainPolicy();
    _o.timeSliceMicros = this.timeSliceMicros();
  }
}

export class FlowNodeT implements flatbuffers.IGeneratedObject {
  constructor(
    public nodeId: string | Uint8Array | null = null,
    public pluginId: string | Uint8Array | null = null,
    public methodId: string | Uint8Array | null = null,
    public kind: NodeKind = NodeKind.TRANSFORM,
    public drainPolicy: DrainPolicy = DrainPolicy.DRAIN_UNTIL_YIELD,
    public timeSliceMicros: number = 0,
  ) {}

  pack(builder: flatbuffers.Builder): flatbuffers.Offset {
    const nodeId =
      this.nodeId !== null ? builder.createString(this.nodeId!) : 0;
    const pluginId =
      this.pluginId !== null ? builder.createString(this.pluginId!) : 0;
    const methodId =
      this.methodId !== null ? builder.createString(this.methodId!) : 0;

    return FlowNode.createFlowNode(
      builder,
      nodeId,
      pluginId,
      methodId,
      this.kind,
      this.drainPolicy,
      this.timeSliceMicros,
    );
  }
}
