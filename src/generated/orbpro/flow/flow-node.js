// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { NodeKind } from "./node-kind.js";
import { DrainPolicy } from "../manifest/drain-policy.js";
/**
 * One plugin-backed flow node.
 */
export class FlowNode {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsFlowNode(bb, obj) {
    return (obj || new FlowNode()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  static getSizePrefixedRootAsFlowNode(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new FlowNode()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }
  nodeId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  pluginId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  methodId(optionalEncoding) {
    const offset = this.bb.__offset(this.bb_pos, 8);
    return this.bb.__string(this.bb_pos + offset, optionalEncoding);
  }
  kind() {
    const offset = this.bb.__offset(this.bb_pos, 10);
    return offset
      ? this.bb.readUint8(this.bb_pos + offset)
      : NodeKind.TRANSFORM;
  }
  drainPolicy() {
    const offset = this.bb.__offset(this.bb_pos, 12);
    return offset
      ? this.bb.readUint8(this.bb_pos + offset)
      : DrainPolicy.DRAIN_UNTIL_YIELD;
  }
  timeSliceMicros() {
    const offset = this.bb.__offset(this.bb_pos, 14);
    return offset ? this.bb.readUint32(this.bb_pos + offset) : 0;
  }
  static startFlowNode(builder) {
    builder.startObject(6);
  }
  static addNodeId(builder, nodeIdOffset) {
    builder.addFieldOffset(0, nodeIdOffset, 0);
  }
  static addPluginId(builder, pluginIdOffset) {
    builder.addFieldOffset(1, pluginIdOffset, 0);
  }
  static addMethodId(builder, methodIdOffset) {
    builder.addFieldOffset(2, methodIdOffset, 0);
  }
  static addKind(builder, kind) {
    builder.addFieldInt8(3, kind, NodeKind.TRANSFORM);
  }
  static addDrainPolicy(builder, drainPolicy) {
    builder.addFieldInt8(4, drainPolicy, DrainPolicy.DRAIN_UNTIL_YIELD);
  }
  static addTimeSliceMicros(builder, timeSliceMicros) {
    builder.addFieldInt32(5, timeSliceMicros, 0);
  }
  static endFlowNode(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // node_id
    builder.requiredField(offset, 6); // plugin_id
    builder.requiredField(offset, 8); // method_id
    return offset;
  }
  static createFlowNode(
    builder,
    nodeIdOffset,
    pluginIdOffset,
    methodIdOffset,
    kind,
    drainPolicy,
    timeSliceMicros,
  ) {
    FlowNode.startFlowNode(builder);
    FlowNode.addNodeId(builder, nodeIdOffset);
    FlowNode.addPluginId(builder, pluginIdOffset);
    FlowNode.addMethodId(builder, methodIdOffset);
    FlowNode.addKind(builder, kind);
    FlowNode.addDrainPolicy(builder, drainPolicy);
    FlowNode.addTimeSliceMicros(builder, timeSliceMicros);
    return FlowNode.endFlowNode(builder);
  }
  unpack() {
    return new FlowNodeT(
      this.nodeId(),
      this.pluginId(),
      this.methodId(),
      this.kind(),
      this.drainPolicy(),
      this.timeSliceMicros(),
    );
  }
  unpackTo(_o) {
    _o.nodeId = this.nodeId();
    _o.pluginId = this.pluginId();
    _o.methodId = this.methodId();
    _o.kind = this.kind();
    _o.drainPolicy = this.drainPolicy();
    _o.timeSliceMicros = this.timeSliceMicros();
  }
}
export class FlowNodeT {
  constructor(
    nodeId = null,
    pluginId = null,
    methodId = null,
    kind = NodeKind.TRANSFORM,
    drainPolicy = DrainPolicy.DRAIN_UNTIL_YIELD,
    timeSliceMicros = 0,
  ) {
    this.nodeId = nodeId;
    this.pluginId = pluginId;
    this.methodId = methodId;
    this.kind = kind;
    this.drainPolicy = drainPolicy;
    this.timeSliceMicros = timeSliceMicros;
  }
  pack(builder) {
    const nodeId = this.nodeId !== null ? builder.createString(this.nodeId) : 0;
    const pluginId =
      this.pluginId !== null ? builder.createString(this.pluginId) : 0;
    const methodId =
      this.methodId !== null ? builder.createString(this.methodId) : 0;
    return FlowNode.createFlowNode(
      builder,
      nodeId,
      pluginId,
      methodId,
      this.kind,
      this.drainPolicy,
      this.timeSliceMicros,
    );
  }
}
//# sourceMappingURL=flow-node.js.map
