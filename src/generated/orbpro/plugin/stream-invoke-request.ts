// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from "flatbuffers";

import { DrainPolicy } from "../manifest/drain-policy.js";
import {
  TypedArenaBuffer,
  TypedArenaBufferT,
} from "../stream/typed-arena-buffer.js";

/**
 * Generic stream-based method invocation request.
 */
export class StreamInvokeRequest implements flatbuffers.IUnpackableObject<StreamInvokeRequestT> {
  bb: flatbuffers.ByteBuffer | null = null;
  bb_pos = 0;
  __init(i: number, bb: flatbuffers.ByteBuffer): StreamInvokeRequest {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsStreamInvokeRequest(
    bb: flatbuffers.ByteBuffer,
    obj?: StreamInvokeRequest,
  ): StreamInvokeRequest {
    return (obj || new StreamInvokeRequest()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  static getSizePrefixedRootAsStreamInvokeRequest(
    bb: flatbuffers.ByteBuffer,
    obj?: StreamInvokeRequest,
  ): StreamInvokeRequest {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new StreamInvokeRequest()).__init(
      bb.readInt32(bb.position()) + bb.position(),
      bb,
    );
  }

  /**
   * Stable target method identifier from PluginManifest.
   */
  methodId(): string;
  methodId(optionalEncoding: flatbuffers.Encoding): string | Uint8Array;
  methodId(optionalEncoding?: any): string | Uint8Array {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return this.bb!.__string(this.bb_pos + offset, optionalEncoding);
  }

  /**
   * Input FlatBuffer frames grouped as a vector of descriptors.
   */
  inputs(index: number, obj?: TypedArenaBuffer): TypedArenaBuffer | null {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset
      ? (obj || new TypedArenaBuffer()).__init(
          this.bb!.__indirect(
            this.bb!.__vector(this.bb_pos + offset) + index * 4,
          ),
          this.bb!,
        )
      : null;
  }

  inputsLength(): number {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
  }

  /**
   * Maximum number of output frames the caller is prepared to accept.
   */
  outputStreamCap(): number {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
  }

  /**
   * Drain policy requested for this call.
   */
  drainPolicy(): DrainPolicy {
    const offset = this.bb!.__offset(this.bb_pos, 10);
    return offset
      ? this.bb!.readUint8(this.bb_pos + offset)
      : DrainPolicy.DRAIN_UNTIL_YIELD;
  }

  static startStreamInvokeRequest(builder: flatbuffers.Builder) {
    builder.startObject(4);
  }

  static addMethodId(
    builder: flatbuffers.Builder,
    methodIdOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(0, methodIdOffset, 0);
  }

  static addInputs(
    builder: flatbuffers.Builder,
    inputsOffset: flatbuffers.Offset,
  ) {
    builder.addFieldOffset(1, inputsOffset, 0);
  }

  static createInputsVector(
    builder: flatbuffers.Builder,
    data: flatbuffers.Offset[],
  ): flatbuffers.Offset {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]!);
    }
    return builder.endVector();
  }

  static startInputsVector(builder: flatbuffers.Builder, numElems: number) {
    builder.startVector(4, numElems, 4);
  }

  static addOutputStreamCap(
    builder: flatbuffers.Builder,
    outputStreamCap: number,
  ) {
    builder.addFieldInt32(2, outputStreamCap, 0);
  }

  static addDrainPolicy(
    builder: flatbuffers.Builder,
    drainPolicy: DrainPolicy,
  ) {
    builder.addFieldInt8(3, drainPolicy, DrainPolicy.DRAIN_UNTIL_YIELD);
  }

  static endStreamInvokeRequest(
    builder: flatbuffers.Builder,
  ): flatbuffers.Offset {
    const offset = builder.endObject();
    builder.requiredField(offset, 4); // method_id
    return offset;
  }

  static createStreamInvokeRequest(
    builder: flatbuffers.Builder,
    methodIdOffset: flatbuffers.Offset,
    inputsOffset: flatbuffers.Offset,
    outputStreamCap: number,
    drainPolicy: DrainPolicy,
  ): flatbuffers.Offset {
    StreamInvokeRequest.startStreamInvokeRequest(builder);
    StreamInvokeRequest.addMethodId(builder, methodIdOffset);
    StreamInvokeRequest.addInputs(builder, inputsOffset);
    StreamInvokeRequest.addOutputStreamCap(builder, outputStreamCap);
    StreamInvokeRequest.addDrainPolicy(builder, drainPolicy);
    return StreamInvokeRequest.endStreamInvokeRequest(builder);
  }

  unpack(): StreamInvokeRequestT {
    return new StreamInvokeRequestT(
      this.methodId(),
      this.bb!.createObjList<TypedArenaBuffer, TypedArenaBufferT>(
        this.inputs.bind(this),
        this.inputsLength(),
      ),
      this.outputStreamCap(),
      this.drainPolicy(),
    );
  }

  unpackTo(_o: StreamInvokeRequestT): void {
    _o.methodId = this.methodId();
    _o.inputs = this.bb!.createObjList<TypedArenaBuffer, TypedArenaBufferT>(
      this.inputs.bind(this),
      this.inputsLength(),
    );
    _o.outputStreamCap = this.outputStreamCap();
    _o.drainPolicy = this.drainPolicy();
  }
}

export class StreamInvokeRequestT implements flatbuffers.IGeneratedObject {
  constructor(
    public methodId: string | Uint8Array | null = null,
    public inputs: TypedArenaBufferT[] = [],
    public outputStreamCap: number = 0,
    public drainPolicy: DrainPolicy = DrainPolicy.DRAIN_UNTIL_YIELD,
  ) {}

  pack(builder: flatbuffers.Builder): flatbuffers.Offset {
    const methodId =
      this.methodId !== null ? builder.createString(this.methodId!) : 0;
    const inputs = StreamInvokeRequest.createInputsVector(
      builder,
      builder.createObjectOffsetList(this.inputs),
    );

    return StreamInvokeRequest.createStreamInvokeRequest(
      builder,
      methodId,
      inputs,
      this.outputStreamCap,
      this.drainPolicy,
    );
  }
}
